{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://tetra.mickelus.se/json/blockstate.schema.json",
    "title": "Blockstate",
    "description": "Vanilla json representation of a blockstate, seems to use capitalized keys.",
    "type": "object",
    "properties": {
        "Name": {
            "$ref": "resource_location.schema.json",
            "$comment": "A resource location pointing at a block (or a #tag?)"
        },
        "Properties": {
            "type": "object",
            "patternProperties": {
                ".*": { "type": "string" }
            },
            "description": "A map of block states (properties?) to match against"
        }
    }
}
