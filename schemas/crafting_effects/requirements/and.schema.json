{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://tetra.mickelus.se/schemas/1.20/crafting_effects/requirements/and.schema.json",
    "title": "And crafting requirement",
    "description": "Passes if ALL of the given requirements pass.",
    "examples": [
        {
            "type": "tetra:and",
            "requirements": [
                { "type": "tetra:never" },
                { "type": "tetra:never" }
            ]
        }
    ],
    "type": "object",
    "properties": {
        "type": {
            "const": "tetra:and"
        },
        "requirements": {
            "type": "array",
            "items": { "$ref": "ref.schema.json" },
            "$comment": "All requirements in this array must pass for this requirement to pass."
        }
    },
    "required": [ "type", "requirements" ]
}
