{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://tetra.mickelus.se/schemas/1.20/crafting_effects/requirements/locked.schema.json",
    "title": "Locked crafting requirement",
    "description": "Passes if some nearby block provides a crafting effect key matching the given key, the providing block typically determines under which conditions it wants to provide its effect(s).",
    "type": "object",
    "properties": {
        "type": {
            "const": "tetra:locked",
            "key": "tetra:cut_efficiency"
        },
        "key": {
            "$ref": "../../resource_location.schema.json",
            "$comment": "This crafting effect key that has to be provided by a nearby block."
        }
    },
    "required": [ "type", "key" ]
}
