{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://tetra.mickelus.se/schemas/1.20/crafting_effects/requirements/improvement.schema.json",
    "title": "Accepts improvement crafting requirement",
    "description": "Passes if the module in the target slot accepts the given improvement, optionally at the given level. todo: change the level field to use an integer predicate (breaking change).",
    "examples": [
        {
            "type": "tetra:improvement",
            "improvement": "blade/serrated",
            "level": 5
        }
    ],
    "type": "object",
    "properties": {
        "type": {
            "const": "tetra:improvement"
        },
        "improvement": {
            "type": "string",
            "description": "Passes if the module in the target slot accepts an improvement with a key matching this value. Accepts as in the module references an improvements resource which define an improvement with the key."
        },
        "level": {
            "$ref": "../../integer_predicate.schema.json",
            "$comment": "The level of the improvement has to match this predicate if present."
        },
        "anySlot": {
            "type": "boolean",
            "description": "If true, any of the modules on the item can fulfill this requirement (not just the module in the target slot)."
        }
    },
    "required": [ "type", "improvement" ]
}
