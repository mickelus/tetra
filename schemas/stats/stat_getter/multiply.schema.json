{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://tetra.mickelus.se/schemas/1.20/stats/stat_getter/multiply.schema.json",
    "title": "Multiply stat getter",
    "description": "A stat getter that multiplies the values of the stats in the given array.",
    "type": "object",
    "properties": {
        "type": {
            "const": "tetra:multiply"
        },
        "stats": {
            "type": "array",
            "items": {
                "$ref": "ref.schema.json"
            },
            "$comment": "Stats from all getters in this array will be multiplied."
        },
        "factor": {
            "type": "number",
            "$comment": "Can be set to multiply the product by a fixed amount."
        }
    },
    "required": [ "stats" ]
}
